# Demo Target for Harbor Review

This repository is a lightweight sample used to test Deni AI Harbor review behavior.

## Setup

1. Install dependencies:

```bash
bun install
```

2. Start app:

```bash
bun run start
```

## Why this repo exists

It contains realistic code patterns that a reviewer should flag (path handling and command execution).

Open a PR with small changes and let Harbor review it.
